ltslam:

  sessions_dir: "/home/fyx/lt-mapper/src/parkinglot_dataset/" # end with / 

  central_sess_name: "01_data" # should be same as the directory name of that session
  query_sess_name: "02_data" # should be same as the directory name of that session
  
  save_directory: "/home/fyx/lt-mapper/src/parkinglot_dataset/data_ltslam/0102/" # end with "/"

  is_display_debug_msgs: false

  loopFitnessScoreThreshold: 0.7

  kNumSCLoopsUpperBound: 1000 # if you use an enough number, use all the detected loops 
  kNumRSLoopsUpperBound: 0 # if you use an enough number, use all the detected loops 

  numberOfCores: 16 # currently hard-coded in the utility.cpp

